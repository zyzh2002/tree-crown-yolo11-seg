# Xi'an platanus instance-segmentation dataset definition.
# `classes` MUST stay identical to the `classes` list published in model.yaml
# (the onboard TensorRtEngine validates the ABI by name against model.yaml).
# The first deployable ABI is fixed by
# .agents/docs/specs/2026-08-07-platanus-other-tree-training-strategy.md.

path: data/                 # dataset root (git-ignored)
train: images/train
val: images/val
test: images/test

names:
  0: platanus
  1: other-tree
